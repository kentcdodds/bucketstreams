.bucket-rules
  for provider in facebook twitter google
    .provider-heading-{provider}
      @extend .color-{provider}

  .provider-accordion-group
    .provider-heading
      font-size 1.6em
    .provider-button
      font-size 2.2em