angular.module('bs.app').controller('BucketCtrl', function($scope, AlertService, CurrentContext) {
//  $scope.bucket = bucketData.bucket;
//  $scope.posts = bucketData.posts;
//  CurrentContext.context($scope.bucket.name);
//
//  $scope.deleteBucket = function() {
//    $scope.bucket.$remove(AlertService.handleResponse.info, AlertService.handleResponse.error);
//  }
});